
update snippet:

git subtree pull --prefix=callkeep {original_sources}/webtrit_callkeep develop
git subtree pull --prefix=tools {original_sources}/webtrit_phone_tools develop
git subtree pull --prefix=analysis {original_sources}/webtrit_analysis main


## because gerrit does not support lfs, we need to export the LFS objects before pulling the subtree.

copy {original_sources}/webtrit_phone {original_sources}/webtrit_phone_delfs 
cd {original_sources}/webtrit_phone_delfs
git checkout develop
git pull origin develop
git lfs fetch --all
git lfs migrate export --everything
rm .gitattributes
cd {this repo path}

git subtree pull --prefix=phone {original_sources}/webtrit_phone_delfs develop
